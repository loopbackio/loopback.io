language: node_js
node_js:
  - "8"

os:
  - linux

script:
  skip

branches:
  only:
  - gh-pages

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: gh-pages
  script:
    - npm run fetch-readmes
  email: slnode@ca.ibm.com
  name: StrongLoop Bot
